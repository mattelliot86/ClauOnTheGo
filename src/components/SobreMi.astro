---
// “Sobre Mí” section.  This component renders a title and a series
// of paragraphs describing the background and motivation behind
// Claudia’s work.  Content is passed via props.

const { content } = Astro.props;
---
<section id="sobre-mi" class="py-16 px-6 md:px-0">
  <h2 class="text-3xl font-bold text-center mb-8 text-gray-800">
    {content.title}
  </h2>
  <div class="max-w-3xl mx-auto space-y-4 text-gray-700">
    {content.content.map((paragraph) => (<p>{paragraph}</p>))}
  </div>
</section>